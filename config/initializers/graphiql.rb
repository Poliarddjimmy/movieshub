# frozen_string_literal: true

# if defined?(GraphiQL)
#   GraphiQL::Rails.config.headers['Authorization'] = ->(context) {
#     "Bearer #{ENV['JWT_TOKEN']}" # rails runner 'puts AuthToken.token(User.first)' to get a token
#   }
# end
